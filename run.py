# Brand_Scope/run.py

# app/__init__.py에서 초기화된 app 객체를 가져옵니다.
# 이 app 객체는 이미 routes.py의 모든 라우트(index, search_analysis)를 등록한 상태입니다.
# from app import app 

# # ----------------------------------------------------
# # 🚀 서버 실행
# # ----------------------------------------------------
# if __name__ == '__main__':
#     # ⚠️ 포트는 8000번, 외부 접속 허용을 위해 host='0.0.0.0'을 사용합니다.
#     app.run(host='0.0.0.0', debug=True, port=8000)